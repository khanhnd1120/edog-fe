export enum GameConfigKey {
  BaseScore,
  ScaleRate,
  StageTime,
  ResetStage,
  ScaleSpdMin,
  ScaleSpdMax,
  ScaleSpdRate,
  BaseHookSpeed,
  HookAngle,
}

export enum Direction {
  Left,
  Right,
}

export enum DialogType {
  DialogEndGame,
}

export enum GameState {
  Prepare,
  Pending,
  GameOver,
}
